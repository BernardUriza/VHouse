@page "/consignments"
@using VHouse.Domain.Entities
@using VHouse.Infrastructure.Data
@using Microsoft.EntityFrameworkCore
@inject VHouseDbContext DbContext
@inject NavigationManager Navigation
@rendermode @(new InteractiveServerRenderMode(false))

<PageTitle>Consignaciones - VHouse</PageTitle>

<div class="consignments-container">
    <div class="page-header">
        <div class="header-content">
            <h1>🤝 Sistema de Consignaciones</h1>
            <p class="subtitle">Gestiona productos en consignación con tus clientes</p>
        </div>
        <div class="header-actions">
            <button class="btn btn-success" @onclick="CreateNewConsignment">
                ➕ Nueva Consignación
            </button>
        </div>
    </div>

    @if (isLoading)
    {
        <div class="loading-container">
            <div class="spinner"></div>
            <p>Cargando consignaciones...</p>
        </div>
    }
    else
    {
        <div class="coming-soon">
            <div class="coming-soon-icon">🚧</div>
            <h2>Sistema en Desarrollo</h2>
            <p>El sistema de consignaciones estará disponible próximamente.</p>
            <p>Preparado para gestionar productos dejados en tiendas con cálculo automático de ganancias.</p>
            
            <div class="features-preview">
                <div class="feature-item">
                    <span class="feature-icon">📊</span>
                    <span>Tracking de ventas por consignación</span>
                </div>
                <div class="feature-item">
                    <span class="feature-icon">💰</span>
                    <span>Cálculo automático de porcentajes</span>
                </div>
                <div class="feature-item">
                    <span class="feature-icon">📈</span>
                    <span>Reportes de liquidaciones</span>
                </div>
                <div class="feature-item">
                    <span class="feature-icon">🔄</span>
                    <span>Control de devoluciones</span>
                </div>
            </div>
        </div>
    }
</div>

<style>
    .consignments-container {
        padding: 2rem;
        max-width: 1400px;
        margin: 0 auto;
        background: var(--background);
        min-height: 100vh;
    }

    .page-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
        padding-bottom: 1.5rem;
        border-bottom: 2px solid var(--accent-primary);
    }

    .header-content h1 {
        color: var(--text-primary);
        margin: 0;
        font-size: 2rem;
        font-weight: 700;
        text-shadow: 0 0 20px var(--accent-glow);
    }

    .subtitle {
        color: var(--text-secondary);
        margin: 0.5rem 0 0;
        font-size: 1.1rem;
    }

    .loading-container {
        text-align: center;
        padding: 4rem;
        color: var(--text-primary);
    }

    .spinner {
        width: 50px;
        height: 50px;
        border: 4px solid var(--background-secondary);
        border-top-color: var(--accent-primary);
        border-radius: 50%;
        animation: spin 1s linear infinite;
        margin: 0 auto 1rem;
        box-shadow: 0 0 20px var(--accent-glow);
    }

    @@keyframes spin {
        to { transform: rotate(360deg); }
    }

    .coming-soon {
        text-align: center;
        padding: 4rem;
        background: var(--background-secondary);
        border-radius: 20px;
        margin: 2rem 0;
        border: 1px solid var(--border-color);
        box-shadow: var(--shadow-lg);
    }

    .coming-soon-icon {
        font-size: 4rem;
        margin-bottom: 1rem;
        filter: drop-shadow(0 0 10px var(--accent-glow));
    }

    .coming-soon h2 {
        color: var(--text-primary);
        margin: 1rem 0;
        font-size: 2rem;
    }

    .coming-soon p {
        color: var(--text-secondary);
        margin: 1rem 0;
        font-size: 1.1rem;
    }

    .features-preview {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1rem;
        margin-top: 2rem;
        max-width: 800px;
        margin-left: auto;
        margin-right: auto;
    }

    .feature-item {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 1rem;
        background: var(--background);
        border: 1px solid var(--border-color);
        border-radius: 12px;
        transition: all 0.3s ease;
    }

    .feature-item:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px var(--accent-glow);
        border-color: var(--accent-primary);
    }

    .feature-icon {
        font-size: 1.5rem;
    }

    .btn {
        padding: 0.75rem 1.5rem;
        border: none;
        border-radius: 12px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
    }

    .btn-success {
        background: linear-gradient(135deg, var(--accent-primary) 0%, var(--accent-secondary) 100%);
        color: var(--text-on-accent);
        box-shadow: 0 0 20px var(--accent-glow);
    }

    .btn-success:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 30px var(--accent-glow);
        filter: brightness(1.1);
    }

    @@media (max-width: 768px) {
        .page-header {
            flex-direction: column;
            align-items: flex-start;
            gap: 1rem;
        }
        
        .features-preview {
            grid-template-columns: 1fr;
        }
    }
</style>

@code {
    private bool isLoading = false;

    private void CreateNewConsignment()
    {
        // Por ahora solo navegar de vuelta, en el futuro será la página de crear consignación
        Navigation.NavigateTo("/consignments");
    }
}

@* Created by Bernard Uriza Orozco *@