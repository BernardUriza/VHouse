@* Created by Bernard Orozco - Modern VHouse Layout *@
@inherits LayoutComponentBase

<div class="page">
    <div class="sidebar">
        <div class="mb-6">
            <div style="color: var(--accent-green); font-size: var(--font-size-lg); font-weight: 700; margin-bottom: var(--spacing-4);">
                🌱 VHouse POS
            </div>
            <p style="font-size: var(--font-size-sm); color: var(--text-muted); margin: 0;">
                Clean Architecture System
            </p>
        </div>
        <NavMenu />
    </div>

    <header class="top-row">
        <div style="margin: 0; font-size: var(--font-size-2xl); font-weight: 700;">
            🌱 VHouse - Clean Architecture POS
        </div>
        <div style="font-size: var(--font-size-sm); color: var(--text-muted);">
            Modern Vegan Point of Sale
        </div>
    </header>

    <main>
        <article class="content">
            @Body
        </article>
    </main>
</div>